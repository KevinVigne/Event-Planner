# Event-Planner
Utilisez la commande suivante pour ajouter le dossier vendor qui contiendra les librairies ici utilisé pour autoload :
-----------------------------------------------------------
|->docker exec -it eventPlanner-php composer dump-autoload